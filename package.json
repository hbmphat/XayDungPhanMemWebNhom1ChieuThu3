{
  "name": "xay-dung-phan-mem-web-nhom-1-chieu-thu-3",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.7.4",
    "turbo": "^2.8.16",
    "typescript": "5.9.2",
    "vite": "^6.0.0"
  },
  "overrides": {
    "vite": "$vite"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@11.8.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "lint-staged": {
    "apps/web-app/**/*.{ts,tsx}": "npx turbo run lint --filter=web-app --",
    "apps/api/**/*.php": "npx turbo run lint --filter=api --"
  }
}
